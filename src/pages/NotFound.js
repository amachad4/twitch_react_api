import React from 'react';

/**
 * Functional react component for HTTP 404 error message
 * 
 * @returns {JSX.Element} - Rendered NotFound component
 */
const NotFound = () => {
    return(
        <h1>HTTP 404</h1>
    );
}

export default NotFound;